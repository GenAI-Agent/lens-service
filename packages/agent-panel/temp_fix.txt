Line 592-593 change from:
      if (success) {
        this.render(); // Re-render to show listening state

To:
      if (success) {
        // Wait a bit for onstart to fire, then re-render
        setTimeout(() => {
          this.render();
        }, 100);
