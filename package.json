{
  "name": "lens-service-v3",
  "version": "1.0.0",
  "private": true,
  "description": "AI Customer Service Widget - Standalone Service",
  "scripts": {
    "install-all": "yarn install && cd agents && yarn install && cd ../server && yarn install && cd ../widget && yarn install && cd ../admin && yarn install",
    "setup": "yarn install-all && yarn prisma generate --schema=./prisma/schema.prisma && yarn prisma db push --schema=./prisma/schema.prisma && yarn build",
    "db:init": "yarn prisma generate --schema=./prisma/schema.prisma && yarn prisma db push --schema=./prisma/schema.prisma",
    "dev": "concurrently \"yarn dev:server\" \"yarn dev:widget\" \"yarn dev:admin\"",
    "dev:server": "cd server && yarn dev",
    "dev:widget": "cd widget && yarn dev",
    "dev:admin": "cd admin && yarn dev",
    "build": "yarn build:widget && yarn build:admin && yarn build:server",
    "build:widget": "cd widget && yarn build",
    "build:admin": "cd admin && yarn build",
    "build:server": "cd server && yarn build",
    "start": "cd server && yarn start",
    "prisma:generate": "yarn prisma generate --schema=./prisma/schema.prisma",
    "prisma:push": "yarn prisma db push --schema=./prisma/schema.prisma",
    "prisma:studio": "yarn prisma studio --schema=./prisma/schema.prisma"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "openai": "^6.9.1"
  }
}
